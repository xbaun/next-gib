query SearchRepo($query: String!) {
    search(query: $query, type: REPOSITORY, first: 10) {
        edges {
            node {
                __typename
                ... on Node {
                    id
                }
                ... on Repository {
                    name
                    url
                }
            }
        }
    }
}
